export enum VisualizationAction {
    PUSH,
    POP,
    INSERT,
    REMOVE,
    CLEAR,
    PLAY,
    RESTART,
    ENABLE_STEP_BY_STEP,
    DISABLE_STEP_BY_STEP,
    NEXT_STEP,
    PUASE,
    RESUME,
    SET_SPEED,
    SORT,
    REROLL,
    SET_NUMBER_OF_ELEMENTS,
    SET_MAX_ELEMENT,
    SET_MIN_ELEMENT
}