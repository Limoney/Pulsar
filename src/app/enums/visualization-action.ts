export enum VisualizationAction {
    PUSH,
    POP,
    INSERT,
    REMOVE,
    CLEAR,
    PLAY,
    RESTART,
    ENABLE_STEP_BY_STEP,
    DISABLE_STEP_BY_STEP,
    NEXT_STEP,
    PUASE,
    RESUME,
    SET_SPEED,
    SORT,
    REROLL
}